# Final validation record

The latest CI run must be the release source. It is required to pass Python tests, Android unit tests, Android debug build, and emulator smoke launch before an APK is described as installable.
