[build-system]
requires=['setuptools>=69']
build-backend='setuptools.build_meta'

[project]
name='universal-creative-agent'
version='0.3.0'
description='Universal AI agent runtime for mobile GUI, browser, creative workflows, and coding'
requires-python='>=3.10'
dependencies=[]

[tool.pytest.ini_options]
pythonpath=['core']
